import React, { FC } from "react";
import * as Styled from "./styled";

type SettingsProps = {};

const Settings: FC<SettingsProps> = () => {
  return <Styled.Container>Settings</Styled.Container>;
};

export default Settings;
